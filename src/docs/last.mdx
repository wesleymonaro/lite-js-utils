---
name: last()
route: /last
---

# last()

Returns the last element from array.

## Params

Just pass the array as param:

```js
import { last } from 'lite-js-utils';

last([0, 1, 2]);
```

## Response

This test will return `2` as value.

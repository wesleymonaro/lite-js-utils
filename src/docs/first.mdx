---
name: first()
route: /first
---

# first()

Returns the first element from array.

## Params

Just pass the array as param:

```js
import { first } from 'lite-js-utils';

first([0, 1, 2]);
```

## Response

This test will return `0` as value.

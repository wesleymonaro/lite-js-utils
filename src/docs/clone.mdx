---
name: clone()
route: /clone
---

# clone()

Allows to clone deep an object or array.

## Params

Just pass the element to copy as param:

```js
import { clone } from 'lite-js-utils';

clone([0, 1, 2]);
```

## Response

This test returns `[0, 1, 2]`.
